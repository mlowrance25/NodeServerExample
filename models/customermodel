var mongoose = require('mongoose');

var Schema = mongoose.Schema;

var CustomerModelSchema = Schema({
    FirstName : String,
    LastName : String,
    Company : String
});

module.exports = mongoose.model('CustomerModel', CustomerModelSchema );
